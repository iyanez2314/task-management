// Enums (shared between frontend and backend)
export * from './lib/enums';

// Interfaces (TypeScript types for frontend)
export type * from './lib/interfaces';

// DTOs (validation classes for backend)
export * from './lib/dtos';

// Entities (TypeORM classes - backend only)
// NOTE: Import these directly from './lib/entities' in backend code only
export * from './lib/entities';
